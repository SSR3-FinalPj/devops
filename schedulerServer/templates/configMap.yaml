apiVersion: v1
kind: ConfigMap
metadata:
  name: airflow-service-config
  namespace: dev-system
data:
  pod_template.yaml: |
    apiVersion: v1
    kind: Pod
    metadata:
      name: airflow-worker-pod-template
    spec:
      serviceAccountName: airflow-service-sa
      restartPolicy: Never
      containers:
      - name: base
        image: 805400277714.dkr.ecr.us-east-2.amazonaws.com/airflow:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "500m"
            memory: "512Mi"
          limits:
            cpu: "1"
            memory: "1Gi"
        env:
        - name: AIRFLOW__CORE__DAGS_FOLDER
          value: /opt/airflow/dags
        - name: PYTHONPATH
          value: /opt/airflow
        - name: AIRFLOW__DATABASE__SQL_ALCHEMY_CONN
          value: postgresql+psycopg2://airflow:airflow@postgres/airflow
        - name: TZ
          value: Asia/Seoul
        envFrom:
          - secretRef:
              name: airflow-service-secrets

        volumeMounts:
          - name: dags-volume
            mountPath: /opt/airflow/dags
      volumes:
      - name: dags-volume
        persistentVolumeClaim:
          claimName: dags-pvc
  airflow.cfg: |
    [core]
    dags_folder = /opt/airflow/dags
    load_examples = False
    [kubernetes]
    worker_container_repository = 805400277714.dkr.ecr.us-east-2.amazonaws.com/airflow
    worker_container_tag = {{ .Values.image.tag }}
    pod_template_file = /opt/airflow/pod_template.yaml
    [kubernetes_executor]
    delete_worker_pods = True
    worker_container_repository = 805400277714.dkr.ecr.us-east-2.amazonaws.com/airflow
    worker_container_tag = {{ .Values.image.tag }}
    pod_template_file = /opt/airflow/pod_template.yaml